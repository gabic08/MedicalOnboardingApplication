@using MedicalOnboardingApplication.Data
@using MedicalOnboardingApplication.ViewModels
@model EditQuestionViewModel

@{
    ViewData["Title"] = "Edit Question";
}

<div class="mb-4">
    <a asp-controller="AdminCourses"
       asp-action="Manage"
       asp-route-id="@Model.CourseId"
       class="btn btn-outline-secondary mb-2">
        <i class="bi bi-arrow-left"></i> Back to Course
    </a>
    <h1 class="mb-0">Edit Question</h1>
</div>

<div class="row">
    <div class="col-12 col-lg-10 mx-auto">
        <form asp-action="Edit" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CourseId" />

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <!-- Question -->
            <div class="card shadow-sm mb-3">
                <div class="card-body">
                    <h6 class="text-muted text-uppercase fw-semibold mb-3">
                        <i class="bi bi-question-circle me-1"></i> Question
                    </h6>
                    <textarea asp-for="QuestionText"
                              class="form-control"
                              rows="3"
                              placeholder="Enter the question..."></textarea>
                    <span asp-validation-for="QuestionText" class="text-danger"></span>
                </div>
            </div>

            <!-- Difficulty -->
            <div class="card shadow-sm mb-3">
                <div class="card-body">
                    <h6 class="text-muted text-uppercase fw-semibold mb-3">
                        <i class="bi bi-bar-chart me-1"></i> Difficulty
                    </h6>
                    <select asp-for="Difficulty"
                            asp-items="Html.GetEnumSelectList<QuestionDifficulty>()"
                            class="form-select">
                    </select>
                </div>
            </div>

            <!-- Answers -->
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h6 class="text-muted text-uppercase fw-semibold mb-1">
                        <i class="bi bi-list-check me-1"></i> Answers
                    </h6>
                    <small class="text-muted d-block mb-3">
                        Select the radio button next to the correct answer.
                        Minimum 2 answers required.
                    </small>

                    <div id="answers-container">
                        @for (int i = 0; i < Model.Answers.Count; i++)
                        {
                            <div class="d-flex align-items-center mb-2 gap-2 answer-row">
                                <div class="form-check mb-0">
                                    <input type="radio"
                                           name="CorrectAnswerIndex"
                                           value="@i"
                                           class="form-check-input"
                                           title="Mark as correct"
                                           @(Model.CorrectAnswerIndex == i ? "checked" : "") />
                                </div>
                                <input type="hidden"
                                       name="Answers[@i].Id"
                                       value="@Model.Answers[i].Id" />
                                <input type="text"
                                       name="Answers[@i].Text"
                                       value="@Model.Answers[i].Text"
                                       class="form-control"
                                       placeholder="Answer @(i + 1)" />
                                <button type="button"
                                        class="btn btn-outline-danger btn-sm remove-answer-btn"
                                        title="Remove">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </div>
                        }
                    </div>

                    <button type="button"
                            id="add-answer-btn"
                            class="btn btn-outline-secondary btn-sm mt-2">
                        <i class="bi bi-plus-circle"></i> Add Answer
                    </button>
                </div>
            </div>

            <div class="d-flex justify-content-end">
                <button type="submit" class="btn btn-primary px-4">
                    <i class="bi bi-save"></i> Save Changes
                </button>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        document.getElementById("add-answer-btn").addEventListener("click", function () {
            const container = document.getElementById("answers-container");
            const index = container.children.length;

            const row = document.createElement("div");
            row.className = "d-flex align-items-center mb-2 gap-2 answer-row";

            row.innerHTML = `
                <div class="form-check mb-0">
                    <input type="radio"
                           name="CorrectAnswerIndex"
                           value="${index}"
                           class="form-check-input"
                           title="Mark as correct" />
                </div>
                <input type="hidden" name="Answers[${index}].Id" value="0" />
                <input type="text"
                       name="Answers[${index}].Text"
                       class="form-control"
                       placeholder="Answer ${index + 1}" />
                <button type="button"
                        class="btn btn-outline-danger btn-sm remove-answer-btn"
                        title="Remove">
                    <i class="bi bi-trash"></i>
                </button>
            `;

            container.appendChild(row);
            attachRemoveHandler(row.querySelector('.remove-answer-btn'));
        });

        function attachRemoveHandler(btn) {
            btn.addEventListener("click", function () {
                this.closest(".answer-row").remove();
                reindexAnswers();
            });
        }

        function reindexAnswers() {
            const rows = document.querySelectorAll(".answer-row");
            rows.forEach((row, i) => {
                row.querySelector("input[type='radio']").value = i;
                row.querySelector("input[type='hidden']").setAttribute("name", `Answers[${i}].Id`);
                row.querySelector("input[type='text']").setAttribute("name", `Answers[${i}].Text`);
                row.querySelector("input[type='text']").setAttribute("placeholder", `Answer ${i + 1}`);
            });
        }

        document.querySelectorAll(".remove-answer-btn").forEach(btn => {
            attachRemoveHandler(btn);
        });
    </script>
}