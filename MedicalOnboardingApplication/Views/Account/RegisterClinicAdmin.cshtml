@using MedicalOnboardingApplication.ViewModels
@model RegisterAdminViewModel
@{
    ViewData["Title"] = "Înregistrare Administrator";
}

<div class="d-flex justify-content-center align-items-center py-5">
    <div class="col-md-7 col-lg-6">
        <div class="card shadow-sm border-0">
            <div class="card-body p-6">

                <div class="text-center mb-4">
                    <div class="bg-success bg-opacity-10 rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                         style="width: 64px; height: 64px;">
                        <i class="bi bi-person-plus fs-3 text-success"></i>
                    </div>
                    <h4 class="fw-semibold mb-0">Creare Cont Administrator</h4>
                    <p class="text-muted small mt-1">Completează detaliile de mai jos pentru înregistrare</p>
                </div>

                <form method="post" novalidate>
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="ReturnUrl" />

                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small mb-3"></div>

                    <div class="row g-3 mb-3">
                        <div class="col-6">
                            <label asp-for="FirstName" class="form-label fw-semibold small"></label>
                            <input asp-for="FirstName" class="form-control" placeholder="Prenume" />
                            <span asp-validation-for="FirstName" class="text-danger small"></span>
                        </div>
                        <div class="col-6">
                            <label asp-for="LastName" class="form-label fw-semibold small"></label>
                            <input asp-for="LastName" class="form-control" placeholder="Nume" />
                            <span asp-validation-for="LastName" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label fw-semibold small"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                            <input asp-for="Email" class="form-control" placeholder="admin@exemplu.com" />
                        </div>
                        <span asp-validation-for="Email" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ClinicName" class="form-label"></label>
                        <input asp-for="ClinicName"
                               class="form-control"
                               placeholder="ex: Clinica Dr. Popescu" />
                        <span asp-validation-for="ClinicName" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Subdomain" class="form-label"></label>
                        <div class="input-group">
                            <input asp-for="Subdomain"
                                   class="form-control"
                                   placeholder="ex: clinica-popescu"
                                   oninput="this.value = this.value.toLowerCase().replace(/[^a-z0-9-]/g, '')" />
                            <span class="input-group-text text-muted small">
                                .@ViewBag.BaseDomain:@ViewBag.BasePort
                            </span>
                        </div>
                        <span asp-validation-for="Subdomain" class="text-danger"></span>
                        <small class="text-muted">Doar litere mici, cifre și cratime.</small>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label fw-semibold small"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-lock"></i></span>
                            <input asp-for="Password"
                                   id="passwordField"
                                   class="form-control"
                                   type="password"
                                   placeholder="Creează o parolă"
                                   autocomplete="new-password" />
                            <button type="button"
                                    class="btn btn-outline-secondary toggle-password"
                                    data-target="passwordField"
                                    tabindex="-1">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ConfirmPassword" class="form-label fw-semibold small"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                            <input asp-for="ConfirmPassword"
                                   id="confirmPasswordField"
                                   class="form-control"
                                   type="password"
                                   placeholder="Repetă parola"
                                   autocomplete="new-password" />
                            <button type="button"
                                    class="btn btn-outline-secondary toggle-password"
                                    data-target="confirmPasswordField"
                                    tabindex="-1">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                        <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                    </div>

                    <button class="btn btn-success w-100 py-2" type="submit">
                        <i class="bi bi-person-check me-1"></i> Creare Cont Administrator
                    </button>

                    <div class="text-center mt-3">
                        <a asp-controller="Account"
                           asp-action="Login"
                           asp-route-returnUrl="@Model.ReturnUrl"
                           class="text-decoration-none text-muted small">
                            <i class="bi bi-arrow-left me-1"></i> Înapoi la Autentificare
                        </a>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.querySelectorAll('.toggle-password').forEach(btn => {
            btn.addEventListener('click', function () {
                const target = document.getElementById(this.dataset.target);
                target.type = target.type === 'password' ? 'text' : 'password';
                this.querySelector('i').classList.toggle('bi-eye');
                this.querySelector('i').classList.toggle('bi-eye-slash');
            });
        });
    </script>
}
